import React from "react";
const Submit = ({ navigation }) => {
  const { go } = navigation;
  return (
    <div>
      <h3>
        Obrigado. Sua retaguarda teste está sendo processada. Aguarde nosso
        contato.
      </h3>
    </div>
  );
};

export default Submit;
